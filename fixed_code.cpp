#include <iostream>
using namespace std;

int main() {
    int n, m, opt, l, r;
    cin >> n >> m;
    cin >> opt >> l >> r;
    
    if (opt == 1) {
        cout << (1 + r) << endl;
    }
    
    return 0;
}
